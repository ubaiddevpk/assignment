let age = prompt("Enter your age ")
if(age>=18&&age<=65)
    {
        alert("you are eligible to cast VOTE")
    }
    else
    {
        alert("Yor are age is " + age + " " + " . You are not eligible to cast VOTE")
    }
