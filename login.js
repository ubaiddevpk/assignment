let x;
let y;
x=prompt("Enter your user name : ")
y=prompt("Enter your user passward : ")
if(x==="obaid"||y==="khan")
    {
        alert("login succesful ")
    }
    else
    {
    alert("invilid user name and passward")
    }